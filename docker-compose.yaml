services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: data-pipelines
    volumes:
      - .:/app
    working_dir: /app
    networks:
      - mobility-network

networks:
  mobility-network:
    driver: bridge
    external: true